package com.bedrockframework.engine.command{	import com.bedrockframework.core.command.*;	import com.bedrockframework.core.event.GenericEvent;	import com.bedrockframework.engine.data.BedrockData;	import com.bedrockframework.engine.manager.*;	import com.bedrockframework.engine.model.Queue;		public class RenderPreloaderCommand extends Command implements ICommand	{		public function RenderPreloaderCommand()		{		}		public  function execute($event:GenericEvent):void		{			var objPreloader:*;			if (AssetManager.hasPreloader(Queue.current.alias)) {				objPreloader=AssetManager.getPreloader(Queue.current.alias);			} else {				objPreloader=AssetManager.getPreloader(BedrockData.DEFAULT_PRELOADER);			}			ContainerManager.buildContainer(BedrockData.PRELOADER_CONTAINER,objPreloader);			PreloaderManager.container = objPreloader;		}	}}